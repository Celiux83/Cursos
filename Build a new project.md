# Build default project

npm init -y

# Build tsconfig.ts file

--> Send to chat.

is the TS compiler options.
It tells TypeScript:
Which files to compile
What JavaScript version to generate
Which module system to use
Where to place compiled files

# Build jest.config.cjs

--> Send to chat.

This is the jest configuration file. It tells jest where to find, run and report tests.
https://jestjs.io/docs/configuration

# Install jest, ts-jest, ts, types/jest, prettier

npm install --save-dev jest ts-jest typescript @types/jest prettier

## Return to presentation & when finished sent src folder

escribe un test de cartService
